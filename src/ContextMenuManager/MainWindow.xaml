<Window
    x:Class="ContextMenuManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ContextMenuManager"
    Title="Context Menu Tool"
    Width="900" Height="650"
    MinWidth="700" MinHeight="500"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#252525"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#333333"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <local:BoolToBrushConverter x:Key="BoolToBrushConverter" TrueBrush="#2D7D46" FalseBrush="#666666"/>
        <local:BoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" Inverse="True"/>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#4A4A4A" CornerRadius="4" Margin="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="#888888"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#383838"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ViewBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBtn" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#383838"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerBtn" TargetType="Button">
            <Setter Property="Background" Value="#C42B1C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="#888888"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBtn" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter TargetName="border" Property="Background" Value="#C42B1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ListItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#1F1F1F" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Context Menu Tool" FontSize="13" Foreground="#CCCCCC" VerticalAlignment="Center"/>
                        <Border Background="#2D7D46" CornerRadius="4" Padding="8,2" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Admin" FontSize="10" Foreground="#FFFFFF"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource TitleBtn}" Click="Minimize_Click">
                            <TextBlock Text="&#x2014;" FontSize="12"/>
                        </Button>
                        <Button x:Name="MaximizeBtn" Style="{StaticResource TitleBtn}" Click="Maximize_Click">
                            <TextBlock x:Name="MaximizeIcon" Text="☐" FontSize="14"/>
                        </Button>
                        <Button Style="{StaticResource CloseBtn}" Click="Close_Click">
                            <TextBlock Text="&#x2715;" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Header -->
            <Grid Grid.Row="1" Margin="24,16,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Mode Toggle -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="NormalModeBtn" Content="Normal" Style="{StaticResource ToggleBtn}" 
                                IsChecked="True" Click="ModeToggle_Click" Tag="Normal"/>
                    <RadioButton x:Name="AdvancedModeBtn" Content="Advanced" Style="{StaticResource ToggleBtn}" 
                                Click="ModeToggle_Click" Tag="Advanced" Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="24,0,0,0">
                    <TextBlock x:Name="HeaderText" Text="Select apps to add to context menu" FontSize="13" Foreground="#777777"/>
                    <TextBlock x:Name="SelectionCount" Text="Use Ctrl+Click to select multiple" FontSize="11" Foreground="#555555" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="RefreshBtn" Style="{StaticResource IconBtn}" Content="↻" Click="Refresh_Click" ToolTip="Refresh"/>
                    <RadioButton x:Name="ListViewBtn" Style="{StaticResource ViewBtn}" Content="☰" Click="ViewToggle_Click" Tag="List" Margin="4,0,0,0"/>
                    <RadioButton x:Name="GridViewBtn" Style="{StaticResource ViewBtn}" Content="⊞" IsChecked="True" Click="ViewToggle_Click" Tag="Grid" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Normal Mode Search -->
            <Border x:Name="SearchPanel" Grid.Row="2" Margin="24,12,24,0" Background="#2A2A2A" CornerRadius="8">
                <Grid>
                    <TextBox x:Name="SearchBox" Style="{StaticResource SearchBox}" 
                            TextChanged="SearchBox_TextChanged" Background="Transparent" BorderThickness="0"
                            VerticalContentAlignment="Center"/>
                    <TextBlock x:Name="Placeholder" Text="Search apps..." Foreground="#555555" FontSize="14" 
                              Margin="16,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Grid>
            </Border>

            <!-- List View -->
            <Border x:Name="ListViewPanel" Grid.Row="3" Background="#252525" Margin="24,12,24,0" CornerRadius="8"
                   Visibility="Collapsed">
                <ListBox x:Name="AppsListBox" Background="Transparent" BorderThickness="0" 
                        SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ItemContainerStyle="{StaticResource ListItem}" SelectionChanged="AppsList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="#333333">
                                    <Image Source="{Binding Icon}" Width="24" Height="24" 
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Ellipse Grid.Column="1" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                        Fill="{Binding IsAdded, Converter={StaticResource BoolToBrushConverter}}"/>
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                    <TextBlock Text="{Binding AddedLocations}" FontSize="11" Foreground="#666666"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Grid View -->
            <Border x:Name="GridViewPanel" Grid.Row="3" Background="#252525" Margin="24,12,24,0" CornerRadius="8"
                   Visibility="Visible">
                <ListBox x:Name="AppsGridBox" Background="Transparent" BorderThickness="0" 
                        SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        SelectionChanged="AppsList_SelectionChanged">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Width" Value="100"/>
                            <Setter Property="Margin" Value="6"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="10">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#0078D4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel HorizontalAlignment="Center" Width="80">
                                <Grid HorizontalAlignment="Center">
                                    <Border Width="48" Height="48" CornerRadius="10" Background="#333333">
                                        <Image Source="{Binding Icon}" Width="32" Height="32"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                            Fill="{Binding IsAdded, Converter={StaticResource BoolToBrushConverter}}"
                                            Stroke="#1A1A1A" StrokeThickness="2"/>
                                </Grid>
                                <TextBlock Text="{Binding DisplayName}" FontSize="11" Foreground="#FFFFFF" 
                                          TextAlignment="Center" TextTrimming="CharacterEllipsis" 
                                          MaxWidth="80" Margin="0,6,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Advanced Panel -->
            <Border x:Name="AdvancedPanel" Grid.Row="3" Background="#252525" Margin="24,12,24,0" CornerRadius="8"
                   Visibility="Collapsed">
                <StackPanel Margin="24">
                    <TextBlock Text="Manually add a context menu entry" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Display Name:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBox x:Name="AdvDisplayName" Grid.Row="0" Grid.Column="1" Background="#2A2A2A" Foreground="#FFFFFF" 
                                BorderThickness="0" Padding="12,10" FontSize="13" Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Command:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBox x:Name="AdvCommand" Grid.Row="1" Grid.Column="1" Background="#2A2A2A" Foreground="#FFFFFF" 
                                BorderThickness="0" Padding="12,10" FontSize="13" Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Icon Path:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,0,0"/>
                        <TextBox x:Name="AdvIconPath" Grid.Row="2" Grid.Column="1" Background="#2A2A2A" Foreground="#FFFFFF" 
                                BorderThickness="0" Padding="12,10" FontSize="13"/>
                    </Grid>

                    <TextBlock Text="Leave Command empty to create a folder-type entry (opens parent folder)" FontSize="11" Foreground="#666666" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Bottom Actions -->
            <Border Grid.Row="4" Background="#1F1F1F" Padding="24,14" CornerRadius="0,0,8,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel x:Name="ContextTypePanel" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Add to:" Foreground="#888888" FontSize="13" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <RadioButton x:Name="EverywhereBtn" Content="Everywhere" Style="{StaticResource ToggleBtn}" 
                                    IsChecked="True" Click="ContextType_Click" Tag="Everywhere"/>
                        <RadioButton x:Name="FileBtn" Content="File" Style="{StaticResource ToggleBtn}" 
                                    Click="ContextType_Click" Tag="File" Margin="8,0,0,0"/>
                        <RadioButton x:Name="FolderBtn" Content="Folder" Style="{StaticResource ToggleBtn}" 
                                    Click="ContextType_Click" Tag="Folder" Margin="8,0,0,0"/>
                        <RadioButton x:Name="BackgroundBtn" Content="Background" Style="{StaticResource ToggleBtn}" 
                                    Click="ContextType_Click" Tag="Background" Margin="8,0,0,0"/>
                    </StackPanel>

                    <Button x:Name="RemoveBtn" Grid.Column="1" Content="Remove Selected" Style="{StaticResource DangerBtn}" 
                           Click="RemoveSelected_Click" IsEnabled="False" Margin="0,0,12,0"/>
                    <Button x:Name="AddBtn" Grid.Column="2" Content="Add Selected" Style="{StaticResource PrimaryBtn}" 
                           Click="AddSelected_Click" IsEnabled="False"/>
                    <Button x:Name="AdvAddBtn" Grid.Column="3" Content="Add Entry" Style="{StaticResource PrimaryBtn}" 
                           Click="AdvAddEntry_Click" Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Footer -->
            <Border Grid.Row="5" Background="#1A1A1A" Padding="0,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Built by " Foreground="#888888" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock x:Name="DevLink" Text="Neel Sapariya" Foreground="#4A90D9" FontSize="11" 
                              Cursor="Hand" VerticalAlignment="Center" FontWeight="SemiBold"
                              MouseLeftButtonDown="Developer_Click"
                              MouseEnter="DevLink_MouseEnter" MouseLeave="DevLink_MouseLeave"/>
                    <TextBlock Text=" • " Foreground="#555555" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text="View on GitHub" Foreground="#4A90D9" FontSize="11" 
                              Cursor="Hand" VerticalAlignment="Center"
                              MouseLeftButtonDown="Developer_Click"
                              MouseEnter="DevLink_MouseEnter" MouseLeave="DevLink_MouseLeave"/>
                </StackPanel>
            </Border>
            
            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.RowSpan="6" Visibility="Collapsed" Background="#CC1A1A1A" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#0078D4"/>
                    <TextBlock Text="Loading apps..." Foreground="#FFFFFF" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Custom Dialog Overlay -->
            <Border x:Name="DialogOverlay" Grid.RowSpan="6" Visibility="Collapsed" Background="#CC000000">
                <Border Background="#252525" CornerRadius="12" Padding="0" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       MinWidth="400" MaxWidth="500">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Background="#1F1F1F" CornerRadius="12,12,0,0" Padding="20,16">
                            <Grid>
                                <TextBlock x:Name="DialogTitle" Text="Title" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                <Button x:Name="DialogCloseBtn" Content="✕" HorizontalAlignment="Right" 
                                       Background="Transparent" Foreground="#888888" BorderThickness="0"
                                       FontSize="14" Cursor="Hand" Click="DialogClose_Click"/>
                            </Grid>
                        </Border>
                        
                        <StackPanel Orientation="Horizontal" Margin="20,20">
                            <Border x:Name="DialogIconBorder" Width="40" Height="40" CornerRadius="20" 
                                   Background="#0078D4" Margin="0,0,16,0" VerticalAlignment="Top">
                                <TextBlock x:Name="DialogIcon" Text="✓" FontSize="20" Foreground="#FFFFFF" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="DialogMessage" Text="Message" FontSize="14" Foreground="#CCCCCC" 
                                      TextWrapping="Wrap" MaxWidth="380" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <Border Background="#1F1F1F" CornerRadius="0,0,12,12" Padding="20,16">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="DialogCancelBtn" Content="Cancel" Style="{StaticResource SecondaryBtn}" 
                                       Visibility="Collapsed" Click="DialogCancel_Click" Margin="0,0,8,0"/>
                                <Button x:Name="DialogOkBtn" Content="OK" Style="{StaticResource PrimaryBtn}" 
                                       Click="DialogOk_Click"/>
                                <Button x:Name="DialogYesBtn" Content="Yes" Style="{StaticResource PrimaryBtn}" 
                                       Visibility="Collapsed" Click="DialogYes_Click" Margin="0,0,8,0"/>
                                <Button x:Name="DialogNoBtn" Content="No" Style="{StaticResource SecondaryBtn}" 
                                       Visibility="Collapsed" Click="DialogNo_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>
